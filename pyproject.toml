[project]
name = "playpokemon"
version = "0.1.0"
description = "Pokemon agent framework (macOS + mGBA)"
requires-python = ">=3.10"
dependencies = [
  "numpy",
  "Pillow"
]

[tool.pdm]
distribution = false
