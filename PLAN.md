# Pokemon Agent Plan (macOS + mGBA + GBA Emerald)

目标：在 macOS 上使用 mGBA，构建一个可自动启动、具备自适应采集、能联网检索攻略并持续总结经验的宝可梦 Agent。输入为键盘，核心玩法目标为练级、通关、收集综合高手。

## 1. 里程碑与范围
- M0: 可启动 mGBA 并加载 ROM，窗口定位稳定。
- M1: 可采集画面 + 解析基础状态 + 按键执行动作。
- M2: LLM 决策闭环（感知 -> 决策 -> 操作 -> 反馈）。
- M3: 自适应采集 + 联网检索 + 经验总结机制。
- M4: 稳定训练流程与指标追踪。

## 2. 系统架构（模块）
- Game Runner: 启动与窗口管理。
- Observation: 自适应采集（FPS 随场景变化）。
- State Tracker: OCR + UI 识别 -> 结构化状态。
- Policy: LLM 决策与动作规划。
- Action Executor: 键盘输入执行。
- Retrieval: 联网检索与摘要。
- Memory: 经验总结与长期策略库。

## 3. 自适应采集策略
目标：降低 CPU 负担，同时在关键时刻保持响应。

### 采样模式
- 探索/对话: 1 FPS
- 战斗/菜单: 5 FPS
- 关键事件/紧急输入: 10 FPS（短时提升）

### 触发规则（示例）
- 屏幕出现战斗 UI / 血条 -> 切换战斗模式。
- 菜单列表或对话框出现 -> 维持 1-5 FPS。
- 连续 3 帧无变化 -> 自动降采样。

## 4. 自动启动与控制流程
- 通过 CLI 启动 mGBA 并加载 ROM。
- 窗口前置 + 尺寸固定。
- 采集模块绑定窗口坐标（防漂移）。

## 5. 经验总结系统
- 每次会话结束自动写总结：路线、练级点、失败原因、推荐技能与队伍搭配。
- 以可复用结构输出到 `notes/`。
- 支持跨版本复用（通用策略 + 版本差异标注）。

## 6. 联网检索策略
- 触发条件：卡关超过 N 次 / 连续失败 / 任务不明。
- 搜索结果摘要化进入上下文。
- 禁止大段原文导入，保留关键信息（地点、条件、技能、进化要求）。

## 7. 实施步骤
1. 搭建目录结构与配置文件。
2. 建立 mGBA 启动脚本。
3. 实现截图采集 + 裁剪 + 采样调度。
4. 实现键盘输入执行器。
5. 添加基础状态解析（对话框/菜单/战斗检测）。
6. 接入 LLM 决策闭环。
7. 实现检索与总结模块。

## 8. 需要你准备的内容
- 合法的 ROM 文件路径。
- mGBA 已安装。

